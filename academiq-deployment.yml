apiVersion: v1
kind: Service
metadata:
  name: academiq
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: http
    service.beta.kubernetes.io/aws-load-balancer-ssl-cert: arn:aws:acm:us-east-1:499779885172:certificate/5a86e5d6-f4bb-4b84-b8ab-200bb81504eb
    # Only run TLS on the port named "https" below.
    service.beta.kubernetes.io/aws-load-balancer-ssl-ports: "https"
spec:
  selector:
    app: academiq
  ports:
  - name: http
    port: 80
    targetPort: 80
  - name: https
    port: 443
    targetPort: 80
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: academiq-deploy
  labels:
    app: academiq
spec:
  selector:
    matchLabels:
      app: academiq
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: academiq
        tier: frontend
    spec:
      containers:
      - name: academiq
        image: diegosv1/front10:latest
        ports:
        - containerPort: 80